{% extends 'administration/layout.html.twig' %}

{% block SubTitle %}
 - Galerie
{% endblock %}

{% block AdministrationBody %}
{{ FlashMessage|raw }}

{% for image in images %}
    <a href="{{UploadPath}}{{ image.image }}" title="{{ image.title }}">
        <img src="{{UploadPath}}{{ image.thumb }}" alt="{{ image.title }}" />
    </a>
{% else %}
Il n'y a aucun fichier pour l'instant.
{% endfor %}

{% include 'upload/form/upload.html.twig' %}
{% endblock %}